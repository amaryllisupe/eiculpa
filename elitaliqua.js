// Example function or method declaration
function addNonOpaquePixels(image, includeNonOpaquePixels = false) {
    if (includeNonOpaquePixels) {
        // Add non-opaque pixels processing logic here
    } else {
        // Default behavior: treat image as fully opaque
    }
}
