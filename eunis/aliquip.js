// Shortened function name for document.getElementById
const getById = id => document.getElementById(id);

// Usage of the shortened function
const loadingLeaderboard = getById('loading-leaderboard');
